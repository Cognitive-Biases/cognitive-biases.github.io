{
  "name": "cognitive-biases.github.io",
  "private": true,
  "version": "1.0.0",
  "description": "Static website for the Cognitive Biases educational app.",
  "scripts": {
    "build": "node scripts/build.mjs && node scripts/enhance.mjs",
    "validate:data": "node scripts/validate-data.mjs",
    "audit:taxonomy": "node scripts/audit-taxonomy.mjs",
    "check": "npm run validate:data && npm run audit:taxonomy && npm run build && node scripts/check.mjs",
    "dev": "npm run build && npx serve dist"
  },
  "devDependencies": {
    "serve": "^14.2.4"
  }
}
