import { mkdir, readFile, readdir, writeFile } from "node:fs/promises";
import { dirname, join } from "node:path";

const SITE = "https://cognitive-biases.github.io";
const OUT = "dist";
const lensData = JSON.parse(await readFile("data/lens-packs.json", "utf8"));
const professionalData = JSON.parse(await readFile("data/professional-lens-packs.json", "utf8"));
const aiTracks = JSON.parse(await readFile("data/ai-era-patterns.json", "utf8"));
const protocols = JSON.parse(await readFile("data/ai-era-experiment-protocols.json", "utf8"));
const tracker = JSON.parse(await readFile("data/ai-era-research-tracker.json", "utf8"));
const esc = (value = "") => String(value).replace(/[&<>"']/g, (c) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;" })[c]);

function header(current = "") {
  const items = [["Explore", "/explore/"], ["Everyday life", "/everyday/"], ["Lens packs", "/lenses/"], ["Professional", "/professional/"], ["Research", "/research/"]];
  return `<header class="site-header"><a class="brand" href="/"><img src="/assets/icon2.png" width="48" height="48" alt="Cognitive Biases icon"><span>Cognitive<br>Biases</span></a><nav aria-label="Primary">${items.map(([name, url]) => `<a href="${url}"${current === url ? ' aria-current="page"' : ""}>${name}</a>`).join("")}<a class="nav-cta" href="/data/">Data</a></nav></header>`;
}
const footer = `<footer class="site-footer"><div><a class="brand brand--footer" href="/"><img src="/assets/icon2.png" width="40" height="40" alt=""><span>Cognitive Biases</span></a><p>A public guide to cognitive biases, evidence and better decisions.</p></div><div class="footer-links"><a href="/tools/career-decision-review/">Career review</a><a href="/tools/lens-pack-builder/">Lens Pack Builder</a><a href="/ai-era/tracker/">AI-era tracker</a><a href="/tools/decision-audit/">Decision Audit</a><a href="/data/">Data</a></div><p class="fine-print">Educational information, not medical, legal, financial or mental-health advice.</p></footer>`;
function page(title, description, path, body, current = "") {
  const canonical = `${SITE}${path}`;
  const schema = { "@context": "https://schema.org", "@type": "WebPage", name: title, description, url: canonical, isPartOf: { "@type": "WebSite", name: "Cognitive Biases", url: SITE } };
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="theme-color" content="#101622"><title>${esc(title)}</title><meta name="description" content="${esc(description)}"><link rel="canonical" href="${canonical}"><link rel="icon" href="/favicon.png"><meta property="og:title" content="${esc(title)}"><meta property="og:description" content="${esc(description)}"><meta property="og:url" content="${canonical}"><link rel="stylesheet" href="/styles.css"><script type="application/ld+json">${JSON.stringify(schema)}</script></head><body><a class="skip" href="#main">Skip to content</a>${header(current)}<main id="main">${body}</main>${footer}</body></html>`;
}
async function emit(path, html) {
  const target = join(OUT, path.replace(/^\//, ""), "index.html");
  await mkdir(dirname(target), { recursive: true });
  await writeFile(target, html);
}

const career = lensData.packs.find((pack) => pack.slug === "career-decisions");
if (!career) throw new Error("Career Decision Lens Pack is required for the Career Decision Review.");
const careerLensMarkup = career.lenses.map((lens, index) => `<article class="workbench-lens"><p class="kicker">Lens ${index + 1}</p><h3><a href="${lens.url}">${esc(lens.name)}</a></h3><p>${esc(lens.question)}</p><label for="lens-${index}">What does this question change or reveal?</label><textarea id="lens-${index}" name="lens${index}" rows="2"></textarea></article>`).join("");
const careerBody = `<section class="page-hero"><p class="eyebrow">Local-first career decision tool</p><h1>Career Decision Review</h1><p class="lede">Compare a job, promotion, career change or stay/leave decision without reducing it to one salary number or one exciting story. Your draft stays in this browser.</p></section><form class="workbench-form" id="career-review"><section class="section workbench-section"><p class="kicker">1 · Frame</p><h2>Write the decision before the arguments.</h2><label for="career-decision">Decision</label><textarea id="career-decision" name="decision" rows="3" placeholder="Example: Should I stay in my current role or accept the new offer?"></textarea><div class="workbench-two"><div><label for="option-a">Option A</label><input id="option-a" name="optionA" type="text"></div><div><label for="option-b">Option B</label><input id="option-b" name="optionB" type="text"></div></div><label for="criteria">Three criteria that matter most</label><textarea id="criteria" name="criteria" rows="3" placeholder="Learning, compensation, flexibility…"></textarea><div class="workbench-two"><div><label for="preferred">Current favourite</label><input id="preferred" name="preferred" type="text"></div><div><label for="deadline">Decision date</label><input id="deadline" name="deadline" type="date"></div></div></section><section class="section workbench-section"><p class="kicker">2 · Independent view</p><h2>Make your own reference point first.</h2><label for="independent-estimate">What would you value or expect before seeing the recruiter, manager or AI estimate?</label><textarea id="independent-estimate" name="independentEstimate" rows="3"></textarea><label for="outside-view">What happened in comparable career moves?</label><textarea id="outside-view" name="outsideView" rows="3" placeholder="Your past moves, peers, market data, similar roles…"></textarea><label for="against">Strongest evidence against your current favourite</label><textarea id="against" name="against" rows="3"></textarea></section><section class="section workbench-section"><p class="kicker">3 · Five lenses</p><h2>Use questions, not diagnoses.</h2><div class="workbench-grid">${careerLensMarkup}</div></section><section class="section section--ink workbench-section"><p class="kicker">4 · Reduce uncertainty</p><h2>What can you learn before making the expensive move?</h2><label for="small-test">Smallest reversible test</label><textarea id="small-test" name="smallTest" rows="3" placeholder="Shadow the role, negotiate one condition, speak to three peers, run a two-week learning test…"></textarea><label for="stop-condition">What result would make you reject the current favourite?</label><textarea id="stop-condition" name="stopCondition" rows="3"></textarea><label for="next-action">Next action</label><textarea id="next-action" name="nextAction" rows="3"></textarea></section><section class="section workbench-output"><div class="workbench-output-head"><div><p class="kicker">Decision record</p><h2>Your reasoning trail.</h2></div><div><button class="button button--secondary" type="button" id="career-copy">Copy summary</button><button class="button button--secondary" type="button" id="career-reset">Reset</button></div></div><pre id="career-summary" aria-live="polite">Start with the decision above.</pre><p class="fine-print" id="career-save">Draft not yet saved.</p></section></form><section class="section"><p class="kicker">Boundary</p><h2>This tool does not choose your career.</h2><p class="lede">It helps preserve criteria, counterevidence and uncertainty. It is not career, financial or mental-health advice, and it does not diagnose a cognitive bias.</p></section><script type="application/json" id="career-lenses">${JSON.stringify(career.lenses).replaceAll("<", "\\u003c")}</script><script src="/assets/career-decision-review.js" defer></script>`;
await emit("/tools/career-decision-review/", page("Career Decision Review | Cognitive Biases", "A local-first career decision worksheet using cognitive-bias lenses, independent estimates, counterevidence and reversible tests.", "/tools/career-decision-review/", careerBody));

const allPacks = [...lensData.packs, ...professionalData.packs];
const lensMap = new Map();
for (const pack of allPacks) for (const lens of pack.lenses) {
  const key = `${lens.name}|${lens.url}`;
  const entry = lensMap.get(key) || { name: lens.name, url: lens.url, questions: [], packs: [] };
  if (!entry.questions.includes(lens.question)) entry.questions.push(lens.question);
  if (!entry.packs.includes(pack.title)) entry.packs.push(pack.title);
  lensMap.set(key, entry);
}
const allLenses = [...lensMap.values()].sort((a, b) => a.name.localeCompare(b.name));
const templateOptions = allPacks.map((pack) => `<option value="${esc(pack.slug)}">${esc(pack.title)}</option>`).join("");
const lensOptions = allLenses.map((lens, index) => `<label class="builder-lens"><input type="checkbox" name="lens" value="${index}"><span><strong>${esc(lens.name)}</strong><small>${esc(lens.questions[0])}</small></span></label>`).join("");
const builderPayload = { packs: allPacks.map((pack) => ({ slug: pack.slug, title: pack.title, lensNames: pack.lenses.map((lens) => lens.name) })), lenses: allLenses };
const builderBody = `<section class="page-hero"><p class="eyebrow">Build a reusable decision checklist</p><h1>Lens Pack Builder</h1><p class="lede">Create a small task-specific review from published cognitive-bias lenses. Start from a template or build your own. The result stays in your browser and can be copied into a meeting note, AI prompt or team checklist.</p></section><form class="workbench-form" id="lens-builder"><section class="section workbench-section"><p class="kicker">Context</p><h2>What recurring decision are you improving?</h2><div class="workbench-two"><div><label for="builder-title">Pack name</label><input id="builder-title" name="title" type="text" placeholder="Release Readiness Lens Pack"></div><div><label for="builder-template">Start from a template</label><select id="builder-template"><option value="">Blank pack</option>${templateOptions}</select></div></div><label for="builder-context">When should this pack be used?</label><textarea id="builder-context" name="context" rows="2"></textarea><label for="builder-outcome">What should a better decision look like?</label><textarea id="builder-outcome" name="outcome" rows="2"></textarea></section><section class="section workbench-section"><p class="kicker">Lenses</p><h2>Choose up to seven useful questions.</h2><p class="lede"><span id="builder-count">0</span>/7 selected. More lenses do not automatically create more wisdom. They mostly create longer meetings.</p><div class="builder-grid">${lensOptions}</div></section><section class="section workbench-section"><p class="kicker">Guardrails</p><h2>Make the pack operational.</h2><label for="builder-alternative">Strongest alternative or counter-position to require</label><textarea id="builder-alternative" name="alternative" rows="2"></textarea><label for="builder-stop">Stop / escalation condition</label><textarea id="builder-stop" name="stopCondition" rows="2"></textarea><label for="builder-review">Review trigger</label><input id="builder-review" name="reviewTrigger" type="text" placeholder="After the pilot, before approval, when evidence changes…"></section><section class="section workbench-output"><div class="workbench-output-head"><div><p class="kicker">Reusable pack</p><h2>Copy into your workflow.</h2></div><div><button class="button button--secondary" id="builder-copy" type="button">Copy pack</button><button class="button button--secondary" id="builder-reset" type="button">Reset</button></div></div><pre id="builder-summary" aria-live="polite">Choose a context and a few lenses.</pre><p class="fine-print" id="builder-save">Draft not yet saved.</p></section></form><section class="section"><p class="kicker">Use with AI</p><h2>Make the pack an instruction, not an oracle.</h2><p class="lede">Ask an AI agent to run the selected questions, show missing evidence and preserve human ownership of the final decision. Do not ask it to label people as biased.</p></section><script type="application/json" id="builder-data">${JSON.stringify(builderPayload).replaceAll("<", "\\u003c")}</script><script src="/assets/lens-pack-builder.js" defer></script>`;
await emit("/tools/lens-pack-builder/", page("Cognitive Bias Lens Pack Builder | Decision Checklist Tool", "Build a reusable cognitive-bias decision checklist for work, hiring, negotiation, projects, AI-assisted decisions and other recurring tasks.", "/tools/lens-pack-builder/", builderBody));

const trackBySlug = new Map(aiTracks.tracks.map((track) => [track.slug, track]));
const protocolBySlug = new Map(protocols.protocols.map((protocol) => [protocol.slug, protocol]));
const stageIndex = new Map(tracker.stageOrder.map((stage, index) => [stage, index]));
const trackerCards = tracker.entries.map((entry) => {
  const track = trackBySlug.get(entry.track);
  if (!track) throw new Error(`${entry.track}: tracker entry has no AI-era track.`);
  const linkedProtocols = entry.protocolSlugs.map((slug) => protocolBySlug.get(slug)).filter(Boolean);
  const stageSteps = tracker.stageOrder.map((stage) => `<span class="tracker-step${stageIndex.get(stage) <= stageIndex.get(entry.stage) ? " tracker-step--reached" : ""}">${esc(stage)}</span>`).join("");
  const protocolLinks = linkedProtocols.length ? `<p><strong>Published protocol:</strong> ${linkedProtocols.map((protocol) => `<a href="/ai-era/protocol/#${protocol.slug}">${esc(protocol.title)}</a>`).join(", ")}</p>` : `<p><strong>Published protocol:</strong> Not yet.</p>`;
  return `<article class="tracker-card"><p class="kicker">${esc(entry.stage)} stage</p><h2><a href="/ai-era/#${entry.track}">${esc(entry.name)}</a></h2><div class="tracker-steps">${stageSteps}</div><p>${esc(entry.whyItMatters)}</p>${protocolLinks}<p><strong>Next milestone:</strong> ${esc(entry.nextMilestone)}</p><p class="fine-print">Track status: ${esc(track.status)}. Research stage and scientific validity are different things.</p></article>`;
}).join("");
const trackerBody = `<section class="page-hero"><p class="eyebrow">Open research pipeline</p><h1>AI-era Research Tracker</h1><p class="lede">Follow working human–AI decision ideas from a falsifiable question to a protocol, documented experiment, result and replication. A stage shows what this project has actually published, not how fashionable the label sounds.</p></section><section class="section"><p class="kicker">Stage model</p><h2>Progress requires an artifact.</h2><div class="tracker-stage-grid">${tracker.stageOrder.map((stage) => `<article><strong>${esc(stage)}</strong><p>${esc(tracker.stageDefinitions[stage])}</p></article>`).join("")}</div><p class="lede">External papers can motivate a track, but they do not move our tracker to “result”. That requires our own documented artifact under the published rules.</p></section><section class="section"><p class="kicker">Current tracks</p><h2>What exists, and what must happen next.</h2><div class="tracker-list">${trackerCards}</div></section><section class="section section--ink"><p class="kicker">Contribute</p><h2>A good research contribution can be a null result.</h2><p class="lede">Useful contributions include a stronger competing explanation, a protocol improvement, a clean run, a replication attempt or evidence that a proposed pattern disappears under better controls.</p><p><a class="button" href="/ai-era/protocol/">Open research protocol</a> <a class="button button--dark" href="/data/ai-era-research-tracker.json">Use tracker data</a></p></section>`;
await emit("/ai-era/tracker/", page("AI-era Research Tracker | Cognitive Biases", "Track human-AI decision hypotheses through idea, protocol, experiment, result and replication stages with explicit evidence boundaries.", "/ai-era/tracker/", trackerBody, "/research/"));

const careerJs = `(() => {\nconst KEY='cognitive-biases-career-review-v1',form=document.getElementById('career-review');if(!form)return;const fields=[...form.elements].filter(x=>x.name),summary=document.getElementById('career-summary'),status=document.getElementById('career-save');const lenses=JSON.parse(document.getElementById('career-lenses')?.textContent||'[]');const read=()=>Object.fromEntries(fields.map(f=>[f.name,f.value]));const write=s=>fields.forEach(f=>{if(Object.hasOwn(s||{},f.name))f.value=s[f.name]||''});const clean=v=>String(v||'').trim();function render(){const s=read(),lines=['CAREER DECISION REVIEW','', 'Decision: '+(clean(s.decision)||'—'),'Option A: '+(clean(s.optionA)||'—'),'Option B: '+(clean(s.optionB)||'—'),'Criteria: '+(clean(s.criteria)||'—'),'Current favourite: '+(clean(s.preferred)||'—'),'Decision date: '+(clean(s.deadline)||'—'),'','INDEPENDENT VIEW','Independent estimate: '+(clean(s.independentEstimate)||'—'),'Outside view: '+(clean(s.outsideView)||'—'),'Evidence against favourite: '+(clean(s.against)||'—'),'','LENSES'];lenses.forEach((l,i)=>lines.push(l.name+': '+(clean(s['lens'+i])||'—')));lines.push('','REDUCE UNCERTAINTY','Smallest reversible test: '+(clean(s.smallTest)||'—'),'Reject favourite if: '+(clean(s.stopCondition)||'—'),'Next action: '+(clean(s.nextAction)||'—'),'','Decision aid only; not a bias diagnosis or career advice.');summary.textContent=lines.join('\\n')}function save(){try{localStorage.setItem(KEY,JSON.stringify(read()));status.textContent='Draft saved locally in this browser.'}catch{status.textContent='Local save is unavailable in this browser.'}}let timer;form.addEventListener('input',()=>{render();clearTimeout(timer);timer=setTimeout(save,180)});try{write(JSON.parse(localStorage.getItem(KEY)||'{}'))}catch{}render();document.getElementById('career-copy').addEventListener('click',async()=>{try{await navigator.clipboard.writeText(summary.textContent);status.textContent='Summary copied.'}catch{status.textContent='Copy is unavailable; select the summary manually.'}});document.getElementById('career-reset').addEventListener('click',()=>{if(!confirm('Clear this local career review?'))return;form.reset();localStorage.removeItem(KEY);render();status.textContent='Draft cleared.'});})();`;
await mkdir(join(OUT, "assets"), { recursive: true });
await writeFile(join(OUT, "assets", "career-decision-review.js"), careerJs);

const builderJs = `(() => {\nconst KEY='cognitive-biases-lens-builder-v1',form=document.getElementById('lens-builder');if(!form)return;const data=JSON.parse(document.getElementById('builder-data')?.textContent||'{}'),checks=[...form.querySelectorAll('input[name="lens"]')],summary=document.getElementById('builder-summary'),status=document.getElementById('builder-save'),count=document.getElementById('builder-count'),template=document.getElementById('builder-template');const textFields=[...form.elements].filter(x=>x.name&&x.name!=='lens');const selected=()=>checks.filter(c=>c.checked).map(c=>data.lenses[Number(c.value)]).filter(Boolean);const read=()=>({fields:Object.fromEntries(textFields.map(f=>[f.name,f.value])),selected:checks.filter(c=>c.checked).map(c=>c.value)});function write(s={}){textFields.forEach(f=>{if(Object.hasOwn(s.fields||{},f.name))f.value=s.fields[f.name]||''});checks.forEach(c=>c.checked=(s.selected||[]).includes(c.value))}function render(){const s=Object.fromEntries(textFields.map(f=>[f.name,f.value])),lenses=selected();count.textContent=lenses.length;const lines=['# '+(s.title.trim()||'Custom Decision Lens Pack'),'', 'Use when: '+(s.context.trim()||'—'),'Better decision means: '+(s.outcome.trim()||'—'),'','## Lenses',...lenses.flatMap((l,i)=>[(i+1)+'. '+l.name,'   - '+l.questions[0],'   - Reference: '+l.url]),'','## Guardrails','Strongest alternative: '+(s.alternative.trim()||'—'),'Stop / escalation condition: '+(s.stopCondition.trim()||'—'),'Review trigger: '+(s.reviewTrigger.trim()||'—'),'','Use these as checking questions, not as diagnoses.'];summary.textContent=lines.join('\\n')}function save(){try{localStorage.setItem(KEY,JSON.stringify(read()));status.textContent='Draft saved locally in this browser.'}catch{status.textContent='Local save is unavailable in this browser.'}}function changed(){render();clearTimeout(window.__lensSave);window.__lensSave=setTimeout(save,180)}form.addEventListener('input',e=>{if(e.target.name==='lens'&&checks.filter(c=>c.checked).length>7){e.target.checked=false;status.textContent='Keep the pack to seven lenses or fewer.'}changed()});template.addEventListener('change',()=>{const p=data.packs.find(p=>p.slug===template.value);if(!p)return;checks.forEach(c=>{const l=data.lenses[Number(c.value)];c.checked=p.lensNames.includes(l.name)});if(!form.elements.title.value)form.elements.title.value=p.title;changed()});try{write(JSON.parse(localStorage.getItem(KEY)||'{}'))}catch{}render();document.getElementById('builder-copy').addEventListener('click',async()=>{try{await navigator.clipboard.writeText(summary.textContent);status.textContent='Pack copied.'}catch{status.textContent='Copy is unavailable; select the pack manually.'}});document.getElementById('builder-reset').addEventListener('click',()=>{if(!confirm('Clear this local lens pack?'))return;form.reset();checks.forEach(c=>c.checked=false);localStorage.removeItem(KEY);render();status.textContent='Draft cleared.'});})();`;
await writeFile(join(OUT, "assets", "lens-pack-builder.js"), builderJs);

await mkdir(join(OUT, "data"), { recursive: true });
await writeFile(join(OUT, "data", "ai-era-research-tracker.json"), JSON.stringify(tracker, null, 2) + "\n");

let sitemap = await readFile(join(OUT, "sitemap.xml"), "utf8");
for (const path of ["/tools/career-decision-review/", "/tools/lens-pack-builder/", "/ai-era/tracker/"]) {
  const loc = `${SITE}${path}`;
  if (!sitemap.includes(`<loc>${loc}</loc>`)) sitemap = sitemap.replace("</urlset>", `<url><loc>${loc}</loc><lastmod>${tracker.updatedAt}</lastmod></url></urlset>`);
}
await writeFile(join(OUT, "sitemap.xml"), sitemap);

const homePath = join(OUT, "index.html");
let home = await readFile(homePath, "utf8");
if (!home.includes('class="decision-workbench-home"')) {
  const block = `<section class="section decision-workbench-home"><p class="kicker">Decision workbench</p><h2>Use the library on a real decision.</h2><p class="lede">Review a career choice, build a reusable lens pack for a team, or follow AI-era research from an idea to a reproducible result.</p><div class="application-grid"><article class="application-card"><span>Interactive</span><strong>Career Decision Review</strong><p>Compare options, preserve an independent estimate, challenge the favourite and define a reversible test.</p><a href="/tools/career-decision-review/">Review a career decision →</a></article><article class="application-card"><span>Builder</span><strong>Lens Pack Builder</strong><p>Create a compact checklist from published lenses for a recurring work decision.</p><a href="/tools/lens-pack-builder/">Build a lens pack →</a></article><article class="application-card"><span>Open research</span><strong>AI-era Research Tracker</strong><p>See which human–AI ideas have only a hypothesis, which have a protocol and what would move them forward.</p><a href="/ai-era/tracker/">Track the research →</a></article></div></section>`;
  home = home.replace("</main>", block + "</main>");
  await writeFile(homePath, home);
}

const stylesPath = join(OUT, "styles.css");
let styles = await readFile(stylesPath, "utf8");
if (!styles.includes(".workbench-form{")) {
  styles += `\n.workbench-form{max-width:1100px;margin:0 auto}.workbench-section{padding-top:3rem;padding-bottom:3rem}.workbench-form label{display:block;font-weight:900;margin:.9rem 0 .35rem}.workbench-form input,.workbench-form textarea,.workbench-form select{width:100%;border:var(--line);background:#fff;padding:.75rem;font:inherit;color:var(--ink)}.workbench-two{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:1rem}.workbench-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));border-left:var(--line);border-top:var(--line);margin-top:1.5rem}.workbench-lens{padding:1.2rem;border-right:var(--line);border-bottom:var(--line);background:#fff}.workbench-lens h3{font:1.15rem Archivo Black,sans-serif;margin:.3rem 0}.section--ink .workbench-form label,.section--ink.workbench-section label{color:#fff}.workbench-output{border-top:var(--line)}.workbench-output-head{display:flex;justify-content:space-between;gap:1rem;align-items:end}.workbench-output-head>div:last-child{display:flex;gap:.7rem;flex-wrap:wrap}.workbench-output pre{white-space:pre-wrap;border:var(--line);padding:1rem;background:#fff;min-height:160px;overflow:auto}.builder-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));border-left:var(--line);border-top:var(--line);margin-top:1.5rem}.builder-lens{display:flex!important;gap:.75rem;align-items:flex-start;margin:0!important;padding:1rem;border-right:var(--line);border-bottom:var(--line);background:#fff}.builder-lens input{width:auto!important;margin-top:.3rem}.builder-lens span{display:grid;gap:.25rem}.builder-lens small{font-weight:600}.tracker-stage-grid{display:grid;grid-template-columns:repeat(5,minmax(0,1fr));border-left:var(--line);border-top:var(--line);margin-top:1.5rem}.tracker-stage-grid article{padding:1rem;border-right:var(--line);border-bottom:var(--line);background:#fff}.tracker-stage-grid strong{text-transform:uppercase;font-size:.8rem}.tracker-list{display:grid;gap:1.5rem}.tracker-card{border:var(--line);padding:1.3rem;background:#fff}.tracker-card h2{margin:.35rem 0 1rem}.tracker-steps{display:flex;gap:.35rem;flex-wrap:wrap;margin:.8rem 0 1.2rem}.tracker-step{border:2px solid var(--ink);padding:.2rem .5rem;font-size:.72rem;font-weight:900;text-transform:uppercase;opacity:.35}.tracker-step--reached{background:var(--yellow);opacity:1}@media(max-width:760px){.workbench-two,.workbench-grid,.builder-grid,.tracker-stage-grid{grid-template-columns:1fr}.workbench-output-head{align-items:flex-start;flex-direction:column}}\n`;
  await writeFile(stylesPath, styles);
}

console.log(`Decision workbench generated: career review, lens builder, and ${tracker.entries.length} AI-era tracker entries.`);
