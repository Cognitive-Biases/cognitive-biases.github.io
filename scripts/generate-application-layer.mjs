import { mkdir, readFile, readdir, writeFile } from "node:fs/promises";
import { dirname, join } from "node:path";

const SITE = "https://cognitive-biases.github.io", OUT = "dist";
const packs = JSON.parse(await readFile("data/lens-packs.json", "utf8"));
const ai = JSON.parse(await readFile("data/ai-era-patterns.json", "utf8"));
const esc = (v="") => String(v).replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));
const nav = (current="") => `<header class="site-header"><a class="brand" href="/"><img src="/assets/icon2.png" width="48" height="48" alt="Cognitive Biases icon"><span>Cognitive<br>Biases</span></a><nav aria-label="Primary">${[["Explore","/explore/"],["Everyday life","/everyday/"],["Lens packs","/lenses/"],["Practice","/practice/"],["Research","/research/"]].map(([t,u])=>`<a href="${u}"${current===u?' aria-current="page"':''}>${t}</a>`).join("")}<a class="nav-cta" href="/data/">Data</a></nav></header>`;
const footer = `<footer class="site-footer"><div><a class="brand brand--footer" href="/"><img src="/assets/icon2.png" width="40" height="40" alt=""><span>Cognitive Biases</span></a><p>A public guide to cognitive biases, evidence and better decisions.</p></div><div class="footer-links"><a href="/lenses/">Lens packs</a><a href="/ai-era/">AI-era Bias Lab</a><a href="/research/">Research</a><a href="/partners/">Partnerships</a><a href="/data/">Data</a></div><p class="fine-print">Educational information, not medical, legal, financial or mental-health advice.</p></footer>`;
function page(title,description,path,body,current="") { const canonical=`${SITE}${path}`; const schema={"@context":"https://schema.org","@type":"WebPage",name:title,description,url:canonical,isPartOf:{"@type":"WebSite",name:"Cognitive Biases",url:SITE}}; return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${esc(title)}</title><meta name="description" content="${esc(description)}"><link rel="canonical" href="${canonical}"><link rel="icon" href="/favicon.png"><meta property="og:title" content="${esc(title)}"><meta property="og:description" content="${esc(description)}"><meta property="og:url" content="${canonical}"><link rel="stylesheet" href="/styles.css"><script type="application/ld+json">${JSON.stringify(schema)}</script></head><body><a class="skip" href="#main">Skip to content</a>${nav(current)}<main id="main">${body}</main>${footer}</body></html>`; }
async function emit(path,html){const target=join(OUT,path.replace(/^\//,""),"index.html");await mkdir(dirname(target),{recursive:true});await writeFile(target,html);}
const card=(label,title,text,href)=>`<article class="application-card"><span>${esc(label)}</span><strong>${esc(title)}</strong><p>${esc(text)}</p><a href="${href}">Open →</a></article>`;

const hubCards=(packs.packs||[]).map(p=>card("Decision lens pack",p.title,p.summary,`/lenses/${p.slug}/`)).join("");
await emit("/lenses/",page("Cognitive Bias Lens Packs | Practical Decision Tools","Practical cognitive-bias lens packs for career choices, AI-assisted work, team decisions, projects and information checking.","/lenses/",`<section class="page-hero"><p class="eyebrow">Apply cognitive biases</p><h1>Start with the decision, not the bias name.</h1><p class="lede">Most people do not need another list of bias names. They need a small set of useful questions when a real choice is in front of them. These packs turn the library into practical workflows for career, AI-assisted work, teams, projects and information checking.</p></section><section class="section"><p class="kicker">Decision lens packs</p><h2>Use psychology where a decision actually happens.</h2><p class="lede">A lens is not a diagnosis. It is a question that helps you test an assumption, look for missing evidence or slow down at the right moment.</p><div class="application-grid">${hubCards}</div></section><section class="section section--ink"><p class="kicker">A simple rule</p><h2>Do not hunt for a bias after every mistake.</h2><p class="lede">Write the decision, make an independent estimate, check the strongest alternative, then use a few relevant lenses. Bias labels matter only when they improve the next action.</p><p><a class="button" href="/tools/decision-audit/">Run a Decision Audit</a></p></section><section class="section"><p class="kicker">For teams</p><h2>Turn a pack into reusable decision infrastructure.</h2><p class="lede">These public packs can become meeting templates, review checklists, AI-agent instructions, workshop cards or quality gates. Domain-specific packs can be built around hiring, product discovery, delivery, safety reviews or research.</p><p><a class="button" href="/partners/">Partnership ideas</a></p></section>`,`/lenses/`));

for(const p of packs.packs||[]){
  const uses=p.useWhen.map(x=>`<li>${esc(x)}</li>`).join("");
  const lenses=p.lenses.map(l=>card("Lens",l.name,l.question,l.url)).join("");
  const steps=p.workflow.map((x,i)=>`<article><strong>${i+1}. ${esc(x)}</strong></article>`).join("");
  const body=`<section class="page-hero"><p class="eyebrow">Practical lens pack</p><h1>${esc(p.title)}</h1><p class="lede">${esc(p.summary)}</p></section><section class="section"><p class="kicker">Use it when</p><h2>A short review before a large conclusion.</h2><ul>${uses}</ul></section><section class="section"><p class="kicker">Five lenses</p><h2>Questions worth asking before you commit.</h2><div class="application-grid">${lenses}</div></section><section class="section section--ink"><p class="kicker">10-minute workflow</p><h2>Turn awareness into an action.</h2><div class="feature-list">${steps}</div></section><section class="section"><p class="kicker">Use with AI</p><h2>Ask the model to challenge the frame, not decorate it.</h2><p><strong>Prompt:</strong> “Review this decision without choosing for me. Restate my criteria, show the strongest case against my preferred option, identify missing evidence, flag possible anchors or sunk costs, and finish with the smallest next test that would reduce uncertainty.”</p><p>The goal is not forced disagreement. It is to make assumptions, alternatives and evidence visible before the recommendation feels inevitable.</p></section><section class="section"><p><a class="button" href="/tools/decision-audit/">Open Decision Audit</a> <a class="button button--dark" href="/lenses/">All lens packs</a></p></section>`;
  await emit(`/lenses/${p.slug}/`,page(`${p.title} | Cognitive Biases`,`${p.summary} Includes five decision lenses, a 10-minute workflow and an AI review prompt.`,`/lenses/${p.slug}/`,body,"/lenses/"));
}

const tracks=(ai.tracks||[]).map(t=>{const src=t.sources?.length?t.sources.map(s=>`<li><a href="${s.url}" rel="noopener">${esc(s.title)} (${s.year})</a></li>`).join(""):"<li>No direct source assigned yet. Treat this as a hypothesis, not a finding.</li>";return `<article class="ai-track" id="${t.slug}"><p class="kicker">${esc(t.status)}</p><h2>${esc(t.name)}</h2><p>${esc(t.idea)}</p><p><strong>Related concepts:</strong> ${esc(t.relatedConcepts.join(", "))}.</p><p><strong>Testable prediction:</strong> ${esc(t.testablePrediction)}</p><h3>Starting evidence</h3><ul>${src}</ul></article>`}).join("");
await emit("/ai-era/",page("AI-Era Cognitive Bias Lab | Human–AI Decision Research","Research tracks for AI anchoring, sycophancy loops, cognitive offloading, source-memory blur, synthetic consensus and confidence transfer.","/ai-era/",`<section class="page-hero"><p class="eyebrow">AI-era Bias Lab</p><h1>Study the loop between human judgment and AI behaviour.</h1><p class="lede">AI can introduce a first answer, mirror a user, blur source memory, change mental effort and transfer confidence back into the human decision. This lab tracks those interaction patterns without pretending every useful name is already a validated cognitive bias.</p></section><section class="section"><p class="kicker">Three evidence levels</p><h2>Name carefully. Test aggressively.</h2><div class="feature-list"><article><strong>Established lens</strong><p>A known cognitive effect with a research history.</p></article><article><strong>Emerging interaction pattern</strong><p>Important parts of the human–AI loop have direct evidence, but the combined label is still a working frame.</p></article><article><strong>Testable hypothesis</strong><p>A falsifiable idea that stays a hypothesis until controlled studies support it.</p></article></div></section><section class="section section--ink"><p class="kicker">Why this matters</p><h2>The unit of study is the loop.</h2><p class="lede">A person asks a leading question. A model adapts. The answer becomes new evidence in the person's mind. The next prompt becomes stronger. Studying that feedback loop can be more useful than assigning a long list of labels to either side.</p></section><section class="section"><p class="kicker">Current research tracks</p><h2>Working ideas with explicit status and predictions.</h2>${tracks}</section><section class="section"><p class="kicker">How to propose a new pattern</p><h2>A name comes last.</h2><div class="feature-list"><article><strong>1. Observe</strong><p>Describe the repeated shift without naming it.</p></article><article><strong>2. Map</strong><p>Check whether an established effect already explains it.</p></article><article><strong>3. Predict</strong><p>Write a result that would differ if the pattern is real.</p></article><article><strong>4. Compare</strong><p>Change one important variable and use a control condition.</p></article><article><strong>5. Replicate</strong><p>Repeat across people, tasks, models and settings.</p></article><article><strong>6. Name</strong><p>Add a label only if it improves understanding.</p></article></div></section><section class="section"><p><a class="button" href="/data/ai-era-patterns.json">Use the dataset</a> <a class="button button--dark" href="/experiments/">Experiments Lab</a></p></section>`,`/research/`));

await mkdir(join(OUT,"data"),{recursive:true});
await writeFile(join(OUT,"data","lens-packs.json"),JSON.stringify(packs,null,2)+"\n");
await writeFile(join(OUT,"data","ai-era-patterns.json"),JSON.stringify(ai,null,2)+"\n");

async function walk(dir){const out=[];for(const e of await readdir(dir,{withFileTypes:true})){const p=join(dir,e.name);if(e.isDirectory())out.push(...await walk(p));else if(e.isFile()&&e.name.endsWith(".html"))out.push(p)}return out}
for(const path of await walk(OUT)){let h=await readFile(path,"utf8");const before=h;h=h.replace(/<nav aria-label="Primary">([\s\S]*?)<\/nav>/,(m,inner)=>{if(inner.includes('href="/lenses/"'))return m;if(inner.includes('<a href="/research/">'))return `<nav aria-label="Primary">${inner.replace('<a href="/research/">','<a href="/lenses/">Lens packs</a><a href="/research/">')}</nav>`;if(inner.includes('<a class="nav-cta"'))return `<nav aria-label="Primary">${inner.replace('<a class="nav-cta"','<a href="/lenses/">Lens packs</a><a class="nav-cta"')}</nav>`;return m});if(h!==before)await writeFile(path,h)}
let home=await readFile(join(OUT,"index.html"),"utf8");if(!home.includes('class="application-lab-home"')){const s=`<section class="section application-lab-home"><p class="kicker">Use the knowledge</p><h2>From bias names to reusable decision tools.</h2><p class="lede">Lens packs turn reviewed concepts into short workflows for real tasks. The AI-era Bias Lab tracks newer human–AI interaction patterns as research questions instead of declaring every interesting effect a new bias.</p><div class="application-grid"><article class="application-card"><span>Practical layer</span><strong>Decision lens packs</strong><p>Career, AI work, teams, projects and information checking.</p><a href="/lenses/">Use a lens pack →</a></article><article class="application-card"><span>Research layer</span><strong>AI-era Bias Lab</strong><p>Anchoring loops, sycophancy, offloading, source memory and new testable hypotheses.</p><a href="/ai-era/">Explore the lab →</a></article></div></section>`;home=home.replace("</main>",s+"</main>");await writeFile(join(OUT,"index.html"),home)}
let css=await readFile(join(OUT,"styles.css"),"utf8");if(!css.includes(".application-grid{")){css+=`\n.application-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));border-left:var(--line);border-top:var(--line);margin-top:2rem}.application-card{display:flex;flex-direction:column;gap:.65rem;min-height:220px;padding:1.25rem;border-right:var(--line);border-bottom:var(--line);background:#fff}.application-card>span{font-size:.76rem;font-weight:900;text-transform:uppercase;color:#5a6475}.application-card>strong{font:1.35rem/1.08 Archivo Black,sans-serif;letter-spacing:-.04em}.application-card>a{margin-top:auto;font-weight:900}.application-card:nth-child(odd){box-shadow:inset 0 5px 0 var(--cyan)}.application-card:nth-child(even){box-shadow:inset 0 5px 0 var(--yellow)}.ai-track{max-width:900px;padding:2rem 0;border-bottom:2px solid currentColor}@media(max-width:760px){.application-grid{grid-template-columns:1fr}}\n`;await writeFile(join(OUT,"styles.css"),css)}
let sm=await readFile(join(OUT,"sitemap.xml"),"utf8");for(const x of [{path:"/lenses/",date:packs.updatedAt},...(packs.packs||[]).map(p=>({path:`/lenses/${p.slug}/`,date:packs.updatedAt})),{path:"/ai-era/",date:ai.updatedAt}]){const loc=SITE+x.path;if(!sm.includes(`<loc>${loc}</loc>`))sm=sm.replace("</urlset>",`<url><loc>${loc}</loc><lastmod>${x.date}</lastmod></url></urlset>`)}await writeFile(join(OUT,"sitemap.xml"),sm);
console.log(`Generated ${(packs.packs||[]).length} lens packs and ${(ai.tracks||[]).length} AI-era research tracks.`);
